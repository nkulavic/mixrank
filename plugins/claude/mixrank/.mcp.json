{
  "mcpServers": {
    "mixrank": {
      "command": "${CLAUDE_PLUGIN_ROOT}/scripts/run-mixrank",
      "args": [
        "mcp"
      ],
      "env": {
        "MIXRANK_API_KEY": "${user_config.api_key}",
        "GOOGLE_PLACES_API_KEY": "${user_config.google_places_api_key}"
      }
    }
  }
}
